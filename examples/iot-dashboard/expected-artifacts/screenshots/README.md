# Screenshots placeholder

This directory is intentionally committed without image files.

In a live run, UI evidence such as screenshots could land here, for example:
- `IOT-DASH-001-dashboard-overview.png`
- `IOT-DASH-002-alerts-panel.png`

The static example keeps only the reserved paths so the repo stays lightweight and public-safe.
